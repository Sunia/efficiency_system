<%= form_for(@weather_distance) do |f| %>
  <% if @weather_distance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@weather_distance.errors.count, "error") %> prohibited this weather_distance from being saved:</h2>

      <ul>
      <% @weather_distance.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>